version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile-frontend
    container_name: symptom-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=development